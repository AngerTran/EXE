import 'package:flutter/material.dart';

/// Design tokens — mirror FE `src/design-system/tokens.ts` (Kinetic Tech Dark).
abstract final class AppColors {
  static const background = Color(0xFF0A0E1A);
  static const foreground = Color(0xFFF8F9FA);
  static const card = Color(0xFF0F172A);
  static const border = Color(0xFF1E293B);
  static const primary = Color(0xFF00D9FF);
  static const primaryForeground = Color(0xFF0A0E1A);
  static const secondary = Color(0xFFA855F7);
  static const success = Color(0xFF10B981);
  static const warning = Color(0xFFF59E0B);
  static const destructive = Color(0xFFEC4899);
  static const muted = Color(0xFF64748B);
  static const mutedForeground = Color(0xFF94A3B8);
}
