/**
 * @deprecated Import from `src/design-system` for new code.
 * Re-exports preserve backward compatibility.
 */
export {
  colors,
  fonts,
  spacing,
  borderRadius,
  breakpoints,
  MOBILE_MAX_WIDTH_PX,
  mobile,
  zIndex,
  durations,
  packages,
  credits,
  mobileNav,
  componentClasses,
  shadows,
} from '../design-system';
